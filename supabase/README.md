# Supabase Workspace

Use this directory for backend configuration and server-side assets as the product evolves.

Expected contents over time:

- `migrations/` for schema changes
- `functions/` for privileged workflows and integrations
- `seed/` or equivalent local data bootstrap assets when needed

The first backend implementation step should follow the first real feature spec so the schema reflects an actual workflow rather than generic placeholder tables.
